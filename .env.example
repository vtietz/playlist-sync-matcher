# Example environment overrides for spotify-m3u-sync
# Copy to .env (which is gitignored) and adjust values.

# Spotify OAuth client credentials
SPX__SPOTIFY__CLIENT_ID=your_spotify_client_id
# Optional: override redirect port (defaults to 9876)
# SPX__SPOTIFY__REDIRECT_PORT=9876

# Library configuration (paths can be relative or absolute)
SPX__LIBRARY__PATHS=music;more_music
SPX__LIBRARY__EXTENSIONS=.mp3;.flac;.m4a;.ogg

# Matching configuration
SPX__MATCHING__FUZZY_THRESHOLD=0.80

# Export configuration
SPX__EXPORT__MODE=mirrored
SPX__EXPORT__DIRECTORY=export/playlists
SPX__EXPORT__PLACEHOLDER_EXTENSION=.missing

# Reports directory
SPX__REPORTS__DIRECTORY=export/reports

# Database path
SPX__DATABASE__PATH=data/spotify_sync.db

# Notes:
# 1. Multiple path-like values can be semicolon separated; your tooling may parse and split them before passing to config overrides.
# 2. Boolean coercion examples: SPX__LIBRARY__FOLLOW_SYMLINKS=true
# 3. To force placeholders mode: SPX__EXPORT__MODE=placeholders
